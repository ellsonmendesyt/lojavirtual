CRIA A ESTRUTURA


INICIALIZA O SEQUELIZE




CRIA O BANCO DE DADOS

 sequelize db:create



Gerar o Model de usuarios


sequelize model:generate  --name Usuario  --attributes nome:string, email:string, senha:string


IMPORTA A CONEXAO DA PASTA DE MODELS
const {sequelize} = require('./models');


MANDA CRIAR AS TABELAS COM BASE NO MODEL
async function main(){

    await sequelize.sync();
}



IMPORTAR USUARIO DO MODEL

const {Usuario} = require('./models');


CRIAR A TABELA DE USUARIOS COM MIGRATIONS
sequelize db:migrate





CRIAR O MODEL DE PRODUTOS


nome
descricao
img
desconto
preco
novopreco


sequelize model:generate  --name Produto  --attributes titulo:string, subtitulo:string, img:string, noCarro:boolean, preco:decimal


 

===================================
INSTALAR OWLCAROUSEL NO react
===================================
npm install react-owl-carousel

import OwCarousel from 'react-owl-carousel';

import 'owl.carousel/dist/assets/owl.carousel.min.css';
import 'owl.carousel/dist/assets/owl.theme.default.min.css';



<OwlCarousel>
 <div className
</OwCarousel>




 RECURSOS
 https://stackoverflow.com/questions/44175049/how-to-save-images-for-react-application/44252443






ADICIONAR UM NOVO CAMPO NA TABELA DE PRODUTOS
npx sequelize-cli migration:create --name modify_produtos_add_noCarro_field




'use strict';
module.exports = {
  up: async (queryInterface, Sequelize) => {
    await queryInterface.createTable('produtos', {
      id: {
        type: Sequelize.INTEGER,
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
      },
      titulo: {
        type: Sequelize.STRING,
        allowNull:false,
      },
      subtitulo: {
        type: Sequelize.STRING,
        allowNull:false,
      },
      noCarro:{
        type:Sequelize.BOOLEAN,
        defaultValue:false,
      },
      img: {
        type: Sequelize.STRING
      },
      preco: {
        type: Sequelize.DECIMAL
      },
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE
      }
    });
  },
  down: async (queryInterface, Sequelize) => {
    await queryInterface.dropTable('produtos');
  }
};